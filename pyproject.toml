[tool.pyright]
include = ["scripts/gmail_mock_server", "scripts/analytics", "scripts/langfuse_loader"]
extraPaths = ["scripts/gmail_mock_server"] 

# Add virtual environment path for IDE
venvPath = "."
venv = ".venv"

executionEnvironments = [
  { root = "scripts/gmail_mock_server"},
  { root = "scripts/analytics"},
  { root = "scripts/langfuse_loader"},
]